import { ElementDirectionDirective } from './element-direction.directive';

describe('ElementDirectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementDirectionDirective();
    expect(directive).toBeTruthy();
  });
});
